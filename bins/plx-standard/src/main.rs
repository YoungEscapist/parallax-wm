//! Parallax без необязательного: ни шлема, ни Minecraft, ни мультиюзера.
//!
//! Вся программа — в библиотеке `parallax`; здесь только точка входа. Разница
//! между сборками задаётся набором фич в Cargo.toml рядом, а не кодом.

fn main() -> Result<(), Box<dyn std::error::Error>> {
    parallax::run()
}
