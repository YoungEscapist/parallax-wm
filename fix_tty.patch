--- a/src/udev.rs
+++ b/src/udev.rs
@@ -1,6 +1,6 @@
 use std::{collections::HashMap, time::Duration};
 
+use std::sync::atomic::{AtomicBool, Ordering};
+use std::sync::Arc;
+
 use smithay::{
     backend::{
         allocator::{
@@ -60,35 +60,12 @@ pub fn init_udev(
     let seat_name = session.seat();
     tracing::info!("dawn/udev: seat={}", seat_name);
 
-    // Как niri: suspend libinput сразу если сессия не активна.
-    let session_active = std::fs::read_to_string("/sys/class/tty/tty0/active")
-        .ok()
-        .and_then(|s| s.trim().replace("tty", "").parse::<u32>().ok())
-        .zip(
-            std::fs::read_to_string(format!("/sys/class/tty/{}/active",
-                std::fs::read_to_string("/sys/class/tty/tty0/active")
-                    .unwrap_or_default().trim().to_string()))
-                .ok()
-        )
-        .is_some();
-
-    // Явно активируем текущий VT — seatd пришлёт ActivateSession
-    let current_vt = std::fs::read_to_string("/sys/class/tty/tty0/active")
-        .unwrap_or_default()
-        .trim()
-        .replace("tty", "")
-        .parse::<i32>()
-        .unwrap_or(2) as i32;
-    tracing::info!("dawn/udev: activating VT{}", current_vt);
-    if let Err(e) = session.change_vt(current_vt) {
-        tracing::warn!("dawn/udev: change_vt failed: {:?}", e);
-    }
+    // Флаг: seatd дал нам DRM master (ActivateSession получен)
+    let session_ready = Arc::new(AtomicBool::new(false));
 
     let mut libinput = Libinput::new_with_udev(
         LibinputSessionInterface::from(session.clone()),
     );
     libinput.udev_assign_seat(&seat_name).unwrap();
     let libinput_backend = LibinputInputBackend::new(libinput.clone());
 
     // ── Session notifier ─────────────────────────────────────────────────────
     let mut libinput_for_notifier = libinput.clone();
+    let session_ready_notifier = session_ready.clone();
     event_loop.handle().insert_source(notifier, move |event, _, state| {
         match event {
             SessionEvent::PauseSession => {
                 tracing::info!("dawn/udev: session paused");
+                session_ready_notifier.store(false, Ordering::SeqCst);
                 libinput_for_notifier.suspend();
                 for device in state.udev_devices.values_mut() {
                     device.drm.pause();
                 }
             }
             SessionEvent::ActivateSession => {
                 tracing::info!("dawn/udev: session activated — acquiring DRM master");
+                session_ready_notifier.store(true, Ordering::SeqCst);
                 let _ = libinput_for_notifier.resume();
 
-                let nodes: Vec<_> = state.udev_devices.keys().cloned().collect();
-                for node in nodes {
-                    let mut devices = std::mem::take(&mut state.udev_devices);
-                    if let Some(device) = devices.get_mut(&node) {
+                // Безопасный take: один раз снаружи цикла
+                let mut devices = std::mem::take(&mut state.udev_devices);
+                for device in devices.values_mut() {
                         match device.drm.activate(false) {
                             Ok(()) => tracing::info!("dawn/udev: DRM master acquired"),
                             Err(e) => tracing::warn!("dawn/udev: activate failed: {:?}", e),
                         }
                         for surface in device.surfaces.values_mut() {
                             let _ = surface.compositor.reset_state();
                             let _ = surface.compositor.frame_submitted();
                         }
                         let crtcs: Vec<_> = device.surfaces.keys().cloned().collect();
                         for crtc in crtcs {
                             if let Some(surface) = device.surfaces.get_mut(&crtc) {
                                 let gles = &mut device.gles as *mut GlesRenderer;
                                 unsafe { render_surface(surface, &mut *gles, state); }
                             }
                         }
-                    }
-                    state.udev_devices = devices;
-                }
+                }
+                state.udev_devices = devices;
             }
         }
     }).unwrap();
@@ -130,24 +107,16 @@ pub fn init_udev(
                 state.udev_devices.insert(node, device);
 
                 // dmabuf global (без изменений)
                 if state.dmabuf_global.is_none() {
                     // ... (этот блок без изменений)
                 }
 
-                // ── Первый рендер через Timer ─────────────────────────────────
-                // Ждём 100ms чтобы seatd успел отправить ActivateSession
-                event_loop.handle().insert_source(
-                    Timer::from_duration(Duration::from_millis(100)),
-                    move |_, _, state| {
-                        tracing::info!("dawn/udev: initial render timer fired");
-                        let mut devices = std::mem::take(&mut state.udev_devices);
-                        if let Some(dev) = devices.get_mut(&node) {
-                            let _ = dev.drm.activate(false);
-                            let crtcs: Vec<_> = dev.surfaces.keys().cloned().collect();
-                            for crtc in crtcs {
-                                if let Some(surface) = dev.surfaces.get_mut(&crtc) {
-                                    let gles = &mut dev.gles as *mut GlesRenderer;
-                                    unsafe { render_surface(surface, &mut *gles, state); }
-                                }
-                            }
-                        }
-                        state.udev_devices = devices;
-                        TimeoutAction::Drop
-                    }
-                ).unwrap();
+                // Первый рендер — только после ActivateSession (через DRM notifier).
+                // Timer убран: без DRM master atomic commit вернёт EACCES,
+                // VBlank не придёт, и рендер-цикл не запустится.
+                tracing::info!("dawn/udev: device ready, waiting for ActivateSession");
             }
             Err(e) => tracing::warn!("dawn/udev: skip {:?}: {}", path, e),
         }
     }
