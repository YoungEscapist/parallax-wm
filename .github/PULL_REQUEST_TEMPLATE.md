<!--
Пиши по-русски или по-английски — читается и то, и другое.
Write in English or Russian, whichever is easier.
-->

## Что меняется / What changes

<!-- Одна-две фразы: что теперь происходит иначе, с точки зрения того, кто
     сидит перед экраном. -->

## Зачем / Why

<!-- Ссылка на задачу, если есть. Если правится баг — из-за чего он был, а не
     только где: в этом дереве причина обычно интереснее места. -->

## Как проверено / How it was checked

<!-- Замер, а не убеждение. Годится любое из:
     - `./harness.sh` + `./ctl.py 'shot /tmp/x.png'` — снимок до и после;
     - `cargo test`;
     - живой сеанс на TTY: что нажимал и что увидел.
     Если проверить нельзя (нужен шлем, второй монитор, гость) — так и скажи. -->

## Мелочи, которые тут ловят / The usual traps

- [ ] Комментарии и имена — по-русски, как в остальном дереве (см. CONTRIBUTING).
- [ ] Никаких новых односимвольных кириллических имён (`с`, `р`, `о`…) —
      компилятор ругается `confusable_idents`, и не зря.
- [ ] Сборка обоих бинарей: `./build.sh` (или `cargo build -p plx-minimal`
      и `-p plx-extra` по отдельности — но не `--workspace`).
- [ ] `cargo fmt` по всему дереву **не** гонять: оно не отформатировано
      rustfmt'ом, и получится 28 тысяч строк шума вместо правки.
